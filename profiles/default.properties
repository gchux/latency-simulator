app.startup.latency.enabled=false
app.startup.latency.min=0
app.startup.latency.max=1

app.coldstart.spikeFactor=1

# properties for root endpoint: `/`
app.response.code._root_=200
app.response.latency.enabled._root_=false
app.response.latency.min._root_=0
app.response.latency.max._root_=1
app.response.latency.spikeFactor._root_=1

app.initialization.latency.enabled=false
app.initialization.latency.min=0
app.initialization.latency.max=1
app.initialization.latency.spikeFactor=1

app.web.requests.pendingLatency.max=300
app.web.requests.concurrency.min=10
app.web.requests.concurrency.max=80
